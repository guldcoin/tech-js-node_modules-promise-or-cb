{
  "name": "promise-or-cb",
  "version": "1.0.0",
  "description": "Support both callback and promise style functions.",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/isysd/promise-or-cb"
  },
  "author": "Ira Miller",
  "license": "MIT",
  "readmeFilename": "README.md",
  "scripts": {
    "test": "./node_modules/.bin/istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage",
    "lint": "eslint --env browser *.js* && eslint --env node *.js*",
    "fix": "eslint --fix *.js*",
    "pre-commit": "npm run test && npm run lint"
  },
  "devDependencies": {
    "coveralls": "^3.0.1",
    "eslint": "^4.19.1",
    "eslint-plugin-es5": "^1.3.1",
    "eslint-plugin-import": "^2.12.0",
    "eslint-plugin-json": "^1.2.0",
    "istanbul": "^0.4.5",
    "mocha": "^3.5.3",
    "pre-commit": "^1.2.2"
  },
  "keywords": [
    "promise",
    "callback",
    "async"
  ],
  "pre-commit": [
    "lint",
    "test"
  ]
}
